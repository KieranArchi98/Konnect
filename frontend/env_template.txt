# Frontend Environment Variables Template
# Copy this to frontend/.env and fill in your values

# Backend URL
VITE_BACKEND_URL=http://localhost:8000

# Supabase Configuration
VITE_SUPABASE_URL=https://drfrkpwwednjckzoftuw.supabase.co
REACT_APP_SUPABASE_URL=https://drfrkpwwednjckzoftuw.supabase.co

# Environment
VITE_ENVIRONMENT=development

# OAuth Configuration (you'll need to set these up)
# IMPORTANT: These should be the SAME values as in your backend .env file
# Get these from Google Cloud Console and GitHub Developer Settings
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_from_google_cloud_console
VITE_GITHUB_CLIENT_ID=your_github_oauth_client_id_from_github_developer_settings

# =============================================================================
# SETUP INSTRUCTIONS:
# =============================================================================
#
# 1. Copy this file to frontend/.env
# 2. Replace the OAuth client IDs with your actual values
# 3. The OAuth client IDs should match what you set in your backend .env file
#
# GOOGLE OAUTH SETUP:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google+ API
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client IDs"
# 5. Set Application Type: "Web application"
# 6. Add Authorized Redirect URIs:
#    - http://localhost:5173/auth/callback
#    - http://localhost:8000/auth/oauth/callback
# 7. Copy the Client ID and use it for VITE_GOOGLE_CLIENT_ID
#
# GITHUB OAUTH SETUP:
# 1. Go to https://github.com/settings/developers
# 2. Click "New OAuth App"
# 3. Fill in the form:
#    - Application name: "Agent Dashboard"
#    - Homepage URL: http://localhost:5173
#    - Authorization callback URL: http://localhost:5173/auth/callback
# 4. Copy the Client ID and use it for VITE_GITHUB_CLIENT_ID 